# Hello MondayHero

install nodejs, npm;

clone project: 
`git clone url`

Hero-Site or clone path:
`cd hero-site`

firstRun: 
`npm install` 
or 
`yarn`

Run: 
`npm install`
or
`yarn dev`

serving localhost:3000